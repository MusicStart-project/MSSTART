<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="liked">	

	<select id="likedcheck" parameterType="album.likedVo" resultType="album.likedVo">
    select likedcheck 
    from training_like 
    where music_no=#{music_no} 
      and user_no=#{user_no}
	</select>
	
	<insert id="insertliked" parameterType="album.likedVo">
		INSERT INTO album (
			user_no, music_no, likedcheck
		) VALUES (
			#{user_no},
			#{music_no},
			1
			
		)
	</insert>
	
	<update id="updateliked" parameterType="album.likedVo">
		UPDATE album SET
			likedcheck = #{checked} 
		WHERE
			user_no= #{user_no} and music_no = #{music_no}
	</update>
</mapper>